using System;
// private -- available only within the containing type. to expost these fields, we can use get ans set prpoerties

// public -- can be accessed from anywhere


// protected --- the protected field is available to the current class and any other class that inherits it


// internal -- available anywhere within the containing assembly

// protected internal -- available outside the assembly to any class which is inheriting the containing class

// after compiling a project, assembly is generated. Assembly contains the intermediate language of source code of the prject
//assemblies are 2 types
// executibles -- console, windows applications 
// dll files -- web app , class lib


// base keyword is also used as base constructor to access the base class fields